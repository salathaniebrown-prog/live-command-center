{
  "name": "live-command-center",
  "version": "1.0.0",
  "description": "Real-time dashboard for supercomputer and AI infrastructure monitoring",
  "main": "server.js",
  "scripts": {
    "start": "node scripts/enable-ai-first-routing.js && node scripts/enable-full-live-data.js && node scripts/enforce-observation-gate.js && node scripts/enable-telemetry-integrity.js && node server.js",
    "dev": "nodemon server.js",
    "test": "node --test",
    "check": "node scripts/enable-ai-first-routing.js && node scripts/enable-full-live-data.js && node scripts/enforce-observation-gate.js && node scripts/enable-telemetry-integrity.js && node --check scripts/enable-ai-first-routing.js && node --check scripts/enable-full-live-data.js && node --check scripts/enforce-observation-gate.js && node --check scripts/enable-telemetry-integrity.js && node --check telemetry-integrity.js && node --check public/secure-telemetry-status.js && node --check command-policy.js && node --check server.js && node --check world-os.js && node --check data-spine.js && node --check satellites.js && node --check px4-telemetry.js && node --test",
    "build": "npm run check"
  },
  "keywords": [
    "dashboard",
    "monitoring",
    "supercomputer",
    "ai"
  ],
  "author": "Salathanielbrown SR",
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2",
    "satellite.js": "7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
