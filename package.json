{
  "name": "live-command-center",
  "version": "1.0.0",
  "description": "Real-time dashboard for supercomputer and AI infrastructure monitoring",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check world-os.js && node --check data-spine.js && node --check satellites.js && node --check px4-telemetry.js && node --test",
    "build:wallet": "npm --prefix wallet install --no-audit --no-fund && npm --prefix wallet run build",
    "build": "npm run check && npm run build:wallet"
  },
  "keywords": [
    "dashboard",
    "monitoring",
    "supercomputer",
    "ai"
  ],
  "author": "Salathanielbrown SR",
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2",
    "satellite.js": "7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
