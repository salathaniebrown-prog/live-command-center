{
  "name": "live-command-center",
  "version": "1.0.0",
  "description": "Real-time dashboard for supercomputer and AI infrastructure monitoring",
  "main": "server.js",
  "scripts": {
    "start": "node scripts/enable-full-live-data.js && node server.js",
    "dev": "nodemon server.js",
    "test": "node --test",
    "check": "node scripts/enable-full-live-data.js && node --check server.js && node --check satellites.js && node --test",
    "build": "npm run check"
  },
  "keywords": [
    "dashboard",
    "monitoring",
    "supercomputer",
    "ai"
  ],
  "author": "Salathanielbrown SR",
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2",
    "satellite.js": "7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
