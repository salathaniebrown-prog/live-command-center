{
  "name": "live-command-center",
  "version": "1.0.0",
  "description": "Real-time dashboard for supercomputer and AI infrastructure monitoring",
  "main": "server.js",
  "scripts": {
    "start": "node scripts/enable-ai-first-routing.js && node scripts/enable-full-live-data.js && node scripts/enable-evm-chain-registry.js && node scripts/enable-base-usdc-payments.js && node server.js",
    "dev": "nodemon server.js",
    "test": "node --test test/*.test.js",
    "check": "node scripts/enable-ai-first-routing.js && node scripts/enable-full-live-data.js && node scripts/enable-evm-chain-registry.js && node scripts/enable-base-usdc-payments.js && node --check scripts/enable-ai-first-routing.js && node --check scripts/enable-full-live-data.js && node --check scripts/enable-evm-chain-registry.js && node --check scripts/enable-base-usdc-payments.js && node --check server.js && node --check world-os.js && node --check data-spine.js && node --check state-embedding.js && node --check satellites.js && node --check px4-telemetry.js && node --check evm-chains.js && node --check crypto-payments.js",
    "ci": "npm run check && npm test",
    "build": "npm run ci"
  },
  "keywords": [
    "dashboard",
    "monitoring",
    "supercomputer",
    "ai"
  ],
  "author": "Salathanielbrown SR",
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2",
    "satellite.js": "7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
